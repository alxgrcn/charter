import type { Metadata } from "next";
import { Geist, Geist_Mono, Playfair_Display } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'Charter — Veteran Benefits Navigator',
  description: 'Find out which VA benefits you may qualify for. Free, confidential, built for veterans.',
  openGraph: {
    title: 'Charter — Veteran Benefits Navigator',
    description: 'Find out which VA benefits you may qualify for. Free, confidential.',
    url: 'https://charter-rosy.vercel.app',
    siteName: 'Charter',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} ${playfair.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
